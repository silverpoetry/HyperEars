## Notice

### AI-Generated Code

本 PR 由 **Sisyphus**（OhMyOpenCode agent，model: `big-pickle`）辅助生成，**包含 AI 编排的代码、测试与文档**。

- 协议实现、适配器、测试、文档、PR 报告均由 AI 协助完成
- 所有改动经实机验证（HUAWEI FreeBuds Pro 3，T0018/T0018C）
- 诊断日志（`HyperEars-logs-20260817-031212.txt`、`HyperEars-logs-20260817-032303.txt`）可供审查
- 如需修改或推翻任何实现，请直接指出，我会跟进处理

**请谨慎合入。**

### 仅适用于 HUAWEI FreeBuds Pro 3

固件版本：截至 2026 年 8 月最新版本；其他华为耳机走标准回退。

## Summary

新增 HUAWEI FreeBuds Pro 3（蓝牙名称 `HUAWEI FreeBuds Pro 3`，型号代码 **T0018 / T0018C**）具体型号适配：私有 RFCOMM SPP channel 1 通道，协议确认后开放降噪/关闭/通透三态切换与降噪四档（均衡/舒适/超强/动态）、透传两档（人声增强/均衡）切换，私有左右耳、充电盒电量；声明智慧音频（`com.huawei.smartaudio`）为控制 App，实机验证避让/恢复闭环与多设备状态同步。

## Scope and impact

- protocol：HuaweiFreebudsSppCodec（帧编解码、CRC-8 校验、电池语义、**读写非对称字节序**：读 `level,mode` / 写 `mode,level`）、huawei.freebuds_pro3.anc_level 类型化特性状态
- integration：HuaweiFreebudsPro3Adapter（EXACT_MATCH，私有协议必须，PROTOCOL_HANDSHAKE 传输）+ ProtocolSession + 注册表登记 + ControlAppCatalog 条目 + 2B 03 设备侧变更通知处理（pendingNoiseRefresh → 立即回查 2B 2A）
- system-module：scope.list、关于页面支持列表
- docs：huawei-freebuds-protocol.md、compatibility.md（2.10 华为章节，证据等级提升至实机验证）、control-apps.md、README.md/README_EN.md、CHANGELOG.md、THIRD_PARTY_NOTICES.md
- 不影响其他品牌 Adapter（全量 295 测试通过）

## Evidence

实机验证通过（2026-08-17）：

- **三态切换**：ANC → Off → Transparency 正常，状态通过 `2B 03` + 重读同步
- **ANC 档位**：均衡/舒适/超强/动态 四档确认；设备在 `2B 04` 写确认中返回 `FF` 表示档位不变，语义正确
- **透传档位**：人声增强/均衡 两档确认
- **避让/恢复闭环**：智慧音频打开时让出私有控制（mode=0 发布，私有通道关闭），app 退出后完全恢复（重连 → handshake → modes=7）
- **多设备同步**：耳机侧全局状态共享——任一已连接设备切换模式，耳机通过 `2B 03` 通知所有连接端，各端重读状态同步；RFCOMM channel 1 为单客户端通道（OpenFreebuds 占用时 HyperEars 保持标准回退，属协议限制）
- **控制 App scope/presence**：`com.huawei.smartaudio` 成功被 LSPosed Hook，yieldToVendorControlApp 开关、token 注册与 controller-presence 广播均已确认

诊断日志（`HyperEars-logs-20260817-031212.txt`、`HyperEars-logs-20260817-032303.txt`）可供审查，已脱敏处理。

## Validation

- [x] `testDebugUnitTest`（295 tests，0 failures）
- [x] `:system-module:lintRelease`
- [x] `:system-module:assembleRelease`
- [x] Hardware validation（HUAWEI FreeBuds Pro 3，T0018/T0018C，实机验证）
- [x] Logs, screenshots and Bluetooth addresses are redacted
- [x] Documentation and compatibility status are updated
- [x] Controller apps synchronized（catalog / Adapter / scope.list / 控制权测试 / control-apps.md）

## Risks and rollback

- **RFCOMM channel 1 单客户端限制**：当第三方客户端（如 OpenFreebuds）占用 channel 1 时，HyperEars 无法建立私有通道，保持标准回退——属协议限制，非 bug
- **控制 App 识别**：`com.huawei.smartaudio` 为华为全球版独立 App（iOS 对应 HUAWEI FreeBuds App），非 IoT 平台 `com.huawei.smarthome`（AI Life / 智慧生活）——已通过硬件日志确认并修正
- **读写非对称字节序**：协议层严格按 OpenFreebuds 实现，若上游有变更需同步更新
- **多设备状态同步**：耳机侧全局共享，不区分来源设备——任一设备切换均生效，与官方 App 行为一致
- 无新增后台轮询、广播或常驻通道；控制 App 作用域仅进程存活 Hook，不影响厂商 App 行为
- 回滚：关闭 Huawei FreeBuds Pro 3 Adapter（调试 > 适配器）即回到标准回退，无需卸载
